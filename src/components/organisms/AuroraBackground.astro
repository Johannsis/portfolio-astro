<div aria-hidden="true" class="aurora">
  <div class="blob blob--glacier blob--drift" style="animation-direction: alternate; animation-delay: -2s;"></div>
  <div class="blob blob--lagoon blob--orbit" style="animation-direction: reverse; animation-delay: -8s;"></div>
  <div class="blob blob--cyan blob--drift" style="animation-direction: alternate-reverse; animation-delay: -12s;"></div>
  <div class="blob blob--indigo blob--drift-slow" style="animation-direction: reverse; animation-delay: -15s;"></div>
  <div class="blob blob--violet blob--orbit-slow" style="animation-direction: alternate; animation-delay: -20s;"></div>
  <div class="blob blob--orchid blob--drift" style="animation-direction: alternate-reverse; animation-delay: -10s;"></div>
  <div class="blob blob--rose blob--orbit" style="animation-direction: reverse; animation-delay: -18s;"></div>
  <div class="blob blob--amber blob--drift" style="animation-direction: alternate; animation-delay: -24s;"></div>
</div>

<style>
  .aurora {
    position: fixed;
    inset: 0;
    overflow: hidden;
    pointer-events: none;
    z-index: 0;
  }

  .blob {
    position: absolute;
    border-radius: 50%;
    filter: blur(60px);
    opacity: 0.6;
  }

  /* Sizes & positions */
  .blob--glacier {
    width: 65%; height: 65%;
    left: -8%; top: -10%;
    background: radial-gradient(closest-side circle at center in oklab, var(--aurora-glacier), transparent 100%);
  }
  .blob--lagoon {
    width: 72%; height: 72%;
    right: -8%; top: -8%;
    background: radial-gradient(closest-side circle at center in oklab, var(--aurora-lagoon), transparent 100%);
  }
  .blob--cyan {
    width: 68%; height: 68%;
    bottom: -10%; left: 8%;
    background: radial-gradient(closest-side circle at center in oklab, var(--aurora-cyan), transparent 100%);
  }
  .blob--indigo {
    width: 70%; height: 70%;
    bottom: -8%; right: -6%;
    background: radial-gradient(closest-side circle at center in oklab, var(--aurora-indigo), transparent 100%);
  }
  .blob--violet {
    width: 74%; height: 74%;
    bottom: -8%; left: -6%;
    background: radial-gradient(closest-side circle at center in oklab, var(--aurora-violet), transparent 100%);
  }
  .blob--orchid {
    width: 52%; height: 52%;
    left: -4%; top: 24%;
    background: radial-gradient(closest-side circle at center in oklab, var(--aurora-orchid), transparent 100%);
  }
  .blob--rose {
    width: 48%; height: 48%;
    right: 8%; top: -6%;
    background: radial-gradient(closest-side circle at center in oklab, var(--aurora-rose), transparent 100%);
  }
  .blob--amber {
    width: 46%; height: 46%;
    bottom: -6%; right: 16%;
    background: radial-gradient(closest-side circle at center in oklab, var(--aurora-amber), transparent 100%);
  }

  /* Animations */
  @keyframes blob-drift-kf {
    0%   { transform: translate(0, 0) scale(1); }
    33%  { transform: translate(4%, 6%) scale(1.05); }
    66%  { transform: translate(-3%, 3%) scale(0.97); }
    100% { transform: translate(0, 0) scale(1); }
  }

  @keyframes blob-orbit-kf {
    0%   { transform: rotate(0deg) translate(5%, 3%) rotate(0deg) scale(1); }
    100% { transform: rotate(360deg) translate(5%, 3%) rotate(-360deg) scale(1.08); }
  }

  .blob--drift {
    animation: blob-drift-kf 18s ease-in-out infinite;
  }

  .blob--drift-slow {
    animation: blob-drift-kf 28s ease-in-out infinite;
  }

  .blob--orbit {
    animation: blob-orbit-kf 22s linear infinite;
  }

  .blob--orbit-slow {
    animation: blob-orbit-kf 34s linear infinite;
  }
</style>
